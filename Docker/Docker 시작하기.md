# Docker
### docker 설치
Amazon Linux에 Docker를 설치하는 방법은 다음과 같다. 이 과정은 Amazon Linux 2를 기준으로 설명한다.

#### 1. 시스템 업데이트
```linux
sudo yum update -y
```

#### 2. Docker 설치
```linux
sudo amazon-linux-extras install docker (Amazon Linux 2)
sudo yum install -y docker (Amazon Linux 2023)
```
