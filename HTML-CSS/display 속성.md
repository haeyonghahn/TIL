# inline-block, block, inline   
### block은 다른 element와 아래 방향으로 쌓인다.   
### inline은 다른 element와 옆으로 쌓인다. inline은 contents 크기에 맞게 생성된다.   
### inline-block은 block이지만 inline은 형태로 옆으로 쌓인다. 또한, block 형태이기 때문에 width와 height 속성을 줄 수가 있다.
